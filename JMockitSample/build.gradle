/* 依存関係 */
dependencies {
    // jmockit → junit の順で読み込めば、テストクラスでのRunWith指定をしなくてよくなる
    testCompile group: 'org.jmockit', name: 'jmockit', version: '1.34'
    testCompile group: 'junit', name: 'junit', version: '4.12'
}
